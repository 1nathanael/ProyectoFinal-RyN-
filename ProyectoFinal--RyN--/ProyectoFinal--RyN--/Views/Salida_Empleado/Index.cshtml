@model IEnumerable<ProyectoFinal__RyN__.Models.Salida_Empleado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_Empleado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Salida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Motivo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_Salida)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id_Empleado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Salida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Motivo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_Salida)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Salida }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Salida }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Salida })
        </td>
    </tr>
}

</table>
